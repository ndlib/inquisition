/** @jsx jsx */
// eslint-disable-next-line no-unused-vars
import React from 'react'
import { jsx, Card, Box, Grid, Flex, Heading, Text } from 'theme-ui'
import PropTypes from 'prop-types'

export const MarbleCard = ({
  target,
  label,
  image,
  type,
  children,
  location,
  referal,
  imageService,
  imageRegion,
  onClick,
  showAsList,
  variant,
}) => {
  return null
}

MarbleCard.propTypes = {
  children: PropTypes.node,
  defaultDisplay: PropTypes.string,
  displayReducer: PropTypes.object,
  extraControls: PropTypes.func,
}

export default MarbleCard
